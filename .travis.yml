language: python
python:
    - "2.7"
    - "3.6"

# install dependancies
install:
    - cd API
    - export PIPENV_IGNORE_VIRTUALENVS=1
    - pip install pipenv
    - pipenv install
    - cp .envexample .env
# run tests
script:
    - pipenv shell
    - nosetests test_endpoints.py
